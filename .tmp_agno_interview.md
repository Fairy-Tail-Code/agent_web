# Agno 项目面试介绍

## 一段话介绍项目

这个项目本质上不是单个聊天 Agent，而是一个基于 Agno、FastAPI 和 MCP 搭起来的多 Agent 平台。我主要做的是把 Agent 当成编排层，把真正重计算、强状态、强领域的能力下沉成独立 tool 或 MCP 服务，再通过 AgentOS 统一暴露成标准化接口。当前项目里比较典型的链路包括数据处理 Agent、Word 文档 Agent、办公文档多 Agent Team，以及浏览器自动化和知识库这类可扩展能力。整体架构上，外层有 gateway 做鉴权、限流和统一入口，中间是 AgentOS 负责 agents/teams/workflows 的注册与会话管理，底层再通过 data-mcp、docx-use-mcp、browser 类服务承接真实执行，所以这个项目更像一个可扩展的 Agent 应用平台，而不是单点功能 Demo。

## 面试官可能会问的问题

### 1. 你怎么定义这个项目的核心价值？

我会把它定义成一个 AgentOS 化的平台项目，不是把大模型直接暴露给用户，而是把不同业务能力做成可编排、可注册、可复用的 Agent、Team 和 MCP 服务。这样做的价值是后续加功能时，不需要重写主应用，只要按“tool/server -> agent/team/workflow -> API 注册”这条链路接进去就行。

### 2. 这个项目里的 gateway 主要解决什么问题？

gateway 不是简单反向代理，它承担了统一入口、TLS、限流和鉴权前置的职责。当前实现里用 OpenResty/Nginx 在网关层校验 Supabase JWT，校验通过后把 `X-User-Id`、`X-User-Email`、`X-User-Scopes` 这些内部头注入给后端，后端的 `AuthMiddleware` 只信任网关注入的身份头，不自己重复做一遍 JWT 验证。这样做的好处是：

- 鉴权逻辑集中，前后端和 AgentOS 路由都走同一套入口。
- app 层拿到的是已经标准化的用户上下文，工具层更容易做多租户隔离。
- `/api/`、`/agui/`、`/backend/` 可以在网关统一拆路由，前端 adapter 和后端主应用可以解耦。

如果面试官继续追问，我会补一句：这种设计本质上是在把“用户身份”和“Agent 执行”解耦，Agent 层只关心当前用户是谁，不关心 token 怎么验。

### 3. 多 Agent 在这个项目里是怎么落地的？

这个项目不是为了“看起来高级”才上多 Agent，而是用在职责天然可拆的场景里。最典型的是 `office_team`：Leader 不自己写所有内容，而是负责理解任务、按目标格式路由给 Word、Markdown、PDF 或搜索专家，然后再做结果验收，比如检查文件是否真的生成。这个模式的关键不是多模型并发，而是把“路由、执行、验收”拆开，让每个成员只处理自己最擅长的一段。

### 4. 多 Agent 和单 Agent 相比，你觉得最大的 trade-off 是什么？

优点是职责清晰、扩展性强、提示词边界更稳定，尤其适合办公文档这种强交付场景。代价是编排复杂度会上升，包括成员选择、上下文传递、结果验收和失败重试。这个项目里为了控制复杂度，Leader 的职责被限定得很明确，只做分派和验收，不和成员抢活，这样多 Agent 才不会退化成“多个大提示词互相转述”。

### 5. 你们为什么用了 MCP？什么能力适合 MCP 化？

这个项目里 MCP 主要承接两类能力：

- 重计算或强状态能力，比如数据预处理、模型训练。
- 独立领域平台能力，比如 Word 文档操作。

比如 `data_agent` 不直接在 Agent 进程里跑 pandas/sklearn，而是通过 `data-mcp` 去做；`docx_use_agent` 也不是靠提示词“想象怎么改 Word”，而是走 `docx-use-mcp` 的真实工具。这样做的原因很现实：

- Agent 负责决策，不负责承载重执行。
- 能力服务可以独立部署、独立扩展、独立压测。
- 工具接口稳定后，不同 Agent 可以复用同一套能力。

如果面试官问“什么情况不该做 MCP”，我会回答：轻量、无状态、只服务单个 Agent 的小工具，先做本地 Toolkit 更合适；只有当工具耗资源、依赖复杂环境、需要跨 Agent 复用时，才值得 server 化。

### 6. 你怎么看 MCP 和 skill 的区别？

我理解 skill 更偏“行为层”和“提示层”的复用，解决的是 Agent 应该怎么思考、怎么调用工具、怎么遵循某种操作约束；MCP 更偏“能力层”和“执行层”的复用，解决的是 Agent 具体能调用什么真实能力。这个项目的主线更偏 MCP，因为作者明显更重视把重能力下沉成服务。skill 在这里更像辅助约束，比如浏览器类 Agent 会用本地 skill 或约束模板来规范操作方式，但真正落地执行的还是后面的浏览器服务。

一句话概括就是：skill 决定“怎么做更稳”，MCP 决定“到底能做什么”。

### 7. 这个项目里的 resume 是怎么理解的？

这里的 resume 不是那种完整工作流引擎里的 checkpoint replay，而是基于 Agno 的 `session_id + session_state + db` 做会话续跑和状态恢复。项目里还有一个 `save_session_hook`，会在对话一开始就把 session 提前写入数据库，避免长任务运行时前端查 session 出现 404。也就是说，它已经具备“同一个会话继续跑”的基础能力，但还不是一个带任务图、带精细补偿的工作流恢复系统。

如果面试官问我怎么继续增强，我会说：

- 高价值状态放 `session_state`，不要只放模型历史里。
- 长链路拆成可确认的阶段节点。
- 对高风险步骤加人工确认和可重试点。

这样 resume 才会从“能续聊”升级成“能续任务”。

### 8. memory 和 history 在这个项目里是怎么区分的？

这个项目里每个 Agent 默认会挂自己的 `db` 和 `MemoryManager`，并且开启了 `enable_agentic_memory`。我的理解是：

- history 是当前会话和最近几轮上下文，服务当下这次推理。
- memory 是跨轮沉淀出来的用户偏好、任务事实或长期信息。

源码里还专门把 `store_history_messages` 关掉，说明项目不希望把所有原始聊天记录无限堆进长期记忆，而是更倾向于让 memory 作为“提炼后的长期信息”。这在工程上很重要，因为很多 Agent 项目失败不是能力不够，而是上下文和记忆污染太快。

### 9. 这个项目的 memory 有什么工程化上的意义？

最大的意义是把“用户是谁、历史做过什么、下一轮该延续什么”从提示词层搬到基础设施层。这样新增 Agent 时，不需要每个 Agent 手写一套长期记忆逻辑，统一配置函数就能把默认的 db 和 memory 能力补上。平台化项目里，这种统一补配置非常关键，否则 Agent 数量一多，行为会很快失控。

### 10. HITL 在这个项目里做到了什么程度？

如果实话实说，这个项目已经有 HITL 的接口基础，但还没有做成特别完整的审批流产品。现在更接近“人可以通过前端带着 session 继续介入任务，并且系统保留会话、身份和中间状态”，而不是“每个高风险动作都内建审批节点”。不过它的架构是适合继续加 HITL 的，因为：

- 有 gateway 和用户身份体系，知道当前是谁在操作。
- 有 session/state/memory，能保存人工介入前后的上下文。
- 有 Team 和 MCP 分层，可以把人工确认插在 Leader 决策前、工具执行前或结果交付前。

如果让我继续做，我会优先把以下场景做成强 HITL：

- 写文件、删文件、覆盖文件前确认。
- 外部系统提交类操作前确认。
- 多 Agent 交付给用户前增加 Leader 或人工验收节点。

### 11. 你觉得这个项目最有代表性的设计思路是什么？

我会总结成一句话：Agent 负责编排，服务负责执行，gateway 负责身份和入口，memory/session 负责连续性。这个分层做对了，项目才有可能从两个 Agent 长成一个平台。

### 12. 如果面试官问“这个项目现在最明显的不足是什么”，你会怎么答？

我会直接讲三个点：

- 文档、测试和当前注册链路存在一定漂移，说明项目还在快速演进。
- workflow 和浏览器能力有设计沉淀，但不一定都在当前主链路默认启用。
- HITL 和 resume 现在更多是基础能力具备，还没有完全产品化。

但我会补一句，这些并不一定是坏事。对于 Agent 平台项目来说，真正重要的是分层和扩展方向已经对了，后续是把实验能力逐步收敛成主链路的问题。

## 面试时可以直接说的结尾总结

如果用一句更面试化的话总结，我会说：我做的不是一个单点 Agent，而是一个面向业务场景的 Agent 平台，重点在于把模型能力、工具能力、状态管理和用户身份体系拆开，让 Agent 只做决策和编排，把重能力通过 MCP 服务化，把长任务通过 session/memory 连续化，再通过 gateway 和 Team 机制把系统做成可扩展、可治理、可继续演进的架构。
